# MVU HTML Display 🖥️

[MVU] - **MaenVaerU** Custom Nodes Suite

## Описание
MVU HTML Display — это продвинутый узел для ComfyUI, позволяющий отображать HTML и CSS контент **непосредственно внутри рабочего пространства** без использования внешних оверлеев или плавающих окон. Узел использует архитектуру "Маски" (Iron Mask), обеспечивая полную стабильность при масштабировании (зуме), перемещении и изменении размера, что было главной проблемой других аналогичных решений (например, MTB Note Plus).

В режиме просмотра узел полностью трансформируется в заданный HTML-интерфейс, оставляя видимой только шапку узла и порты для контроля.

## Ключевые особенности
*   **Стабильность (Zero Drift):** Используется нативная интеграция с LiteGraph, что исключает "плавание" или смещение HTML при масштабировании.
*   **Два режима:** Переключение между режимом **Просмотра (View Mode)** и режимом **Редактирования (Edit Mode)** без перезагрузки.
*   **Чистый UI:** В режиме просмотра узел становится "кирпичом" из вашего HTML. Стандартные поля ввода полностью скрыты.
*   **Color Picker:** Встроенная кнопка 🎨 для быстрого задания цвета фона.
*   **Сохранность данных:** Все данные (HTML, CSS) хранятся в самом узле и сохраняются вместе с вашим рабочим процессом (workflow.json).

## Установка
1.  Перейдите в папку `ComfyUI/custom_nodes`.
2.  Клонируйте репозиторий или создайте папку `MVU-HTML-Display`:

    ```bash
    cd ComfyUI/custom_nodes
    git clone https://github.com/MaenVaerU/comfyui-html-display.git
    ```
3.  Перезапустите ComfyUI.

## Использование
1.  Нажмите **Add Node** -> **MVU/Visualization** -> **MVU HTML Display 🖥️**.
2.  По умолчанию узел находится в режиме **Просмотра (View)**.
3.  Нажмите на кнопку с иконкой **✏️** (Карандаш) в правом нижнем углу. Узел переключится в режим **Редактирования**.
4.  В режиме Редактирования:
    *   **HTML Content:** Введите свой HTML код.
    *   **CSS Styles:** Введите свои стили. **ВАЖНО:** Для стилизации основного фона используйте селектор `body { ... }`.
5.  Нажмите кнопку **👁️** (Глаз) или снова **✏️**, чтобы вернуться в режим Просмотра.
6.  Для задания цвета фона используйте кнопку **🎨** в нижнем правом углу.

## Файлы проекта
*   `html_display_nodes.py`: Серверная логика (Python). Определяет структуру узла и его входы/выходы.
*   `__init__.py`: Регистрация узла в ComfyUI.
*   `web/js/mvu_html_display.js`: Клиентская логика (JavaScript). Управляет отрисовкой, переключением режимов и синхронизацией данных.
*   `requirements.txt`: Список внешних зависимостей (в данном проекте пуст).